/** @format */

const HelloWorldOne = ({ dateTime }) => {
  return (
    <div style={{ color: "red", fontSize: "24px", fontWeight: "bold" }}>
      Hello World! - {dateTime}
    </div>
  );
};

export default HelloWorldOne;
